name: opensearch-advanced

pre_install_actions:
  
project_files:
- docker-compose.opensearch.yaml
- opensearch/Dockerfile

global_files:

depencencies:

post_install_actions:

removal_actions:

yaml_read_files:

